import React from 'react';
import Layout from '../../layout/Layout';

const UserPage = ({ ...props }) => {
  return (
    <Layout {...props}>
      <div>User</div>
    </Layout>
  );
};

export default UserPage;
